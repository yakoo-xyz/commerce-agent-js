{
  "name": "commerce-agent-js",
  "private": true,
  "version": "0.1.0",
  "description": "AI commerce agent — SDK, server, and embeddable chat widget",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev -w express-server-example",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
